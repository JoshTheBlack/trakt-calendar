# Copy this file to .env (gitignored) for local, non-Docker runs of `python run.py`.
# Docker deployments set real environment variables instead — see README.md.

# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=
