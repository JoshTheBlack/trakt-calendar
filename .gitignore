# Secrets / credentials
config.php
.env
*.php

# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# App runtime data (settings.json with API keys, per-month watch state, detail cache)
data/

# Local scratch/test data dirs (mirrors of data/, never part of the repo).
# data-*/ covers the throwaway copies made to exercise a month rollover against
# real data without writing to it — see TRAKT_FAKE_TODAY in README. Those carry a
# real settings.json, so an unignored one is API keys a commit away from leaving.
scratch_data/
data-*/

# Claude Code local (personal) settings
.claude/settings.local.json

# Build/design notes (personal, not part of the release)
Plans/
PLANNING.md

# OS / editor junk
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
