# Secrets / credentials
config.php
.env
*.php

# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# App runtime data (settings.json with API keys, per-month watch state, detail cache)
data/

# Local scratch/test data dir (mirror of data/, never part of the repo)
scratch_data/

# Claude Code local (personal) settings
.claude/settings.local.json

# Build/design notes (personal, not part of the release)
BUILD_PLAN.txt
BUILD_SECURITY.txt
BUILD_OG_IMAGE.txt
FEATURE_SIMKL.txt
PLANNING.md

# OS / editor junk
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
